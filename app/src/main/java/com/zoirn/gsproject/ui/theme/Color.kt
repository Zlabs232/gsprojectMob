package com.zoirn.gsproject.ui.theme

import androidx.compose.ui.graphics.Color

// === Dark Theme (default) ===
// Background: #0c0f1a, Accent: #8070ff, Text: #f1f4ff
val DarkBackground = Color(0xFF0C0F1A) //основной фон — тёмно-синий
val DarkSurface = Color(0xFF141829) //поверхность карточек — чуть светлее фона
val DarkSurfaceVariant = Color(0xFF1C2038) //вариант поверхности — ещё чуть светлее
val DarkAccent = Color(0xFF8070FF) //основной акцентный цвет — фиолетово-синий
val DarkAccentVariant = Color(0xFF6B5CE7) //тёмный вариант акцента
val DarkText = Color(0xFFF1F4FF) //основной цвет текста — почти белый с синим оттенком
val DarkTextSecondary = Color(0xFFB0B5D0) //вторичный текст — серо-синий
val DarkOnAccent = Color(0xFFFFFFFF) //текст на акцентном фоне — белый
val DarkError = Color(0xFFFF6B6B) //цвет ошибок — красный
val DarkSuccess = Color(0xFF4ADE80) //цвет успеха — зелёный
val DarkOutline = Color(0xFF2A2E45) //цвет рамок и разделителей

// === Pink Theme ===
// Background: #1a0a1f, Accent: #ec4899, Text: #ffe4f5
val PinkBackground = Color(0xFF1A0A1F) //основной фон — тёмно-фиолетовый с тёплым оттенком
val PinkSurface = Color(0xFF241430) //поверхность карточек
val PinkSurfaceVariant = Color(0xFF2E1C3A) //вариант поверхности
val PinkAccent = Color(0xFFEC4899) //основной акцент — ярко-розовый
val PinkAccentVariant = Color(0xFFD63B87) //тёмный вариант розового
val PinkText = Color(0xFFFFE4F5) //основной текст — розовато-белый
val PinkTextSecondary = Color(0xFFCBA8C4) //вторичный текст — розово-серый
val PinkOnAccent = Color(0xFFFFFFFF) //текст на розовом фоне — белый
val PinkError = Color(0xFFFF6B8A) //ошибки — розово-красный
val PinkSuccess = Color(0xFF4ADE80) //успех — зелёный (одинаковый в обеих темах)
val PinkOutline = Color(0xFF3D2548) //рамки — тёмно-фиолетовый
